const links=document.querySelectorAll('.settings-link');const sections=document.querySelectorAll('.settings-section');links.forEach(link=>link.addEventListener('click',()=>{links.forEach(x=>x.classList.remove('active'));sections.forEach(x=>x.classList.remove('active'));link.classList.add('active');document.getElementById(link.dataset.section)?.classList.add('active')}));document.querySelectorAll('[data-save]').forEach(button=>button.addEventListener('click',()=>{const original=button.textContent;button.textContent='Saved';button.disabled=true;setTimeout(()=>{button.textContent=original;button.disabled=false},1200)}));document.querySelectorAll('.account-card button').forEach(button=>button.addEventListener('click',()=>{button.textContent='Coming soon';button.disabled=true}));