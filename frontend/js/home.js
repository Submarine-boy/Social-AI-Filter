document.addEventListener('DOMContentLoaded',()=>{const reveal=[...document.querySelectorAll('.section-heading,.feature-card,.steps article,.priority-banner,.pricing-mini article,.final-cta')];reveal.forEach((el,i)=>{el.classList.add('scroll-reveal');el.style.setProperty('--reveal-delay',`${Math.min(i*45,300)}ms`)});if('IntersectionObserver' in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.12,rootMargin:'0px 0px -40px'});reveal.forEach(el=>observer.observe(el))}else reveal.forEach(el=>el.classList.add('is-visible'));const button=document.querySelector('.mobile-menu'),nav=document.querySelector('.site-header nav');button?.addEventListener('click',()=>nav?.classList.toggle('mobile-open'))});